<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" href="public/favicon.ico">
    <link rel="stylesheet" href="public/styles.css" />
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
      integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z"
      crossorigin="anonymous"
    />
    <script src="https://code.jquery.com/jquery-3.4.1.slim.min.js" integrity="sha384-J6qa4849blE2+poT4WnyKhv5vZF5SrPo0iEjwBvKU7imGFAV0wwj1yYfoRSJoZ+n" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js" integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous"></script>
    <title>Ateja FM</title>
  </head>
  <body>
    <div class="row justify-content-center m-3">
      <div class="col-xs-10 col-sm-6">
        <h2>Ateja FM</h2>
        <% if(success === "true") { %>
        <div class="alert alert-success go-away" role="alert">Updated!</div>
        <% } else if(success === "false"){ %>
        <div class="alert alert-danger go-away" role="alert">Failed!</div>
        <% } %>
        <form 
          id="config-form" 
          ref="config-form" 
          action="/update" 
          method="POST" 
          encType="multipart/form-data">
          <div class="form-group">
            <label for="streamUrl">Stream URL</label>
            <input
              name="url"
              value="<%= url %>"
              type="text"
              class="form-control"
              id="streamUrl"
              placeholder="https://some-stream"
            />
          </div>
          <div class="form-group">
            <label for="file">Loop Sound</label>
            <input 
              class="form-control-file"
              type="file"
              name="file" 
              id="file"/>
              <small class="form-text text-muted">The uploaded audio will be used as loop audio.</small>
          </div>
          <div class="form-group">
            <div class="form-check">
              <input
                <% if(isStream) { %>
                  checked
                <% } %>
                name="isStream"
                type="checkbox"
                class="form-check-input"
                id="playStream"
              />
              <label class="form-check-label" for="playStream"
                >Stream audio</label
              >
              <small class="form-text text-muted">If unchecked, selected audio file will be played.</small>
            </div>
          </div>
          <div class="form-group">
            <label for="audioFiles">Selected Loop Audio</label>
            <select class="form-control" id="audioFiles" name="audioFile">
              <% for(var i=0; i < audioFiles.length; i++) { %>
                <option 
                  <% if(filename === audioFiles[i]) { %>
                    selected
                  <% } %>
                  >
                  <%= audioFiles[i] %>
                </option>
              <% } %>
            </select>
            <small class="form-text text-muted">No effect if uploading a file.</small>
            <small class="form-text text-muted">Restart required to take effect.</small>
          </div>
          <div class="form-group">
            <label for="maxVol" id="maxVolLabel">Max Volume</label>
            <input 
              type="range" 
              class="form-control-range" 
              id="maxVol" 
              min="0"
              max="100"
              step="1"
              value="<%= max %>">
              <small class="form-text text-muted">Sets current volume as well.</small>
          </div>
          <div class="form-group">
            <label for="minVol" id="minVolLabel">Min Volume</label>
            <input 
              type="range"
              class="form-control-range" 
              id="minVol" 
              min="0" 
              max="100" 
              step="1"
              value="<%= min %>">
          </div>
          <div class="form-group">
            <label for="volStep" id="volStepLabel">Volume Step</label>
            <input 
              type="range" 
              class="form-control-range" 
              id="volStep" 
              min="1" 
              max="20" 
              step="1"
              value="<%= step %>">
          </div>
          <small class="form-text text-muted">Volumes are applied immediately.</small>
          <button type="submit" class="btn btn-block btn-primary mt-2">
            Update
          </button>
        </form>
      </div>
    </div>
  </body>
  <script type="text/javascript" src="public/script.js"></script>
</html>
